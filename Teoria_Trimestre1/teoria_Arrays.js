/**
 * pop();
 * -Borra la última posición de un array.
 * 
 * shift();
 * -Borra la primera posición de un array.
 * 
 * unshift();
 * -Añade a un array por su primera posición.
 * 
 * reverse();
 * -Da la vuelta a un array.
 * 
 * join();
 * -Añade a todas la posiciones lo que le pasas por parametros.
 * 
 * some();
 * -Comprueba si algún elemento cumple con la función que 
 * se le pasa por parametros.
 * 
 * every();
 * -Lo de some() pero comprueba si todas lo cumplen.
 *  
 */